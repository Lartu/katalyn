$string: "hOla sOy LArtu!";
$changes: table;
$changes{normal}: $string;
$changes{ucase}: ucase($string);
$changes{lcase}: lcase($string);
$changes{ucasefirst}: ucasefirst($string);
$changes{title}: title($string);
$changes{substr}: substr($string, -80, 3);

print($changes{ucase});